<Window x:Class="MacAccents.View.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MacAccents – Settings"
        Icon="pack://application:,,,/Assets/app.ico"
        SizeToContent="Height"
        Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <StackPanel Margin="20">

        <TextBlock Text="Hold delay before the accent popup appears"
                   FontWeight="SemiBold"/>
        <TextBlock Text="How long a key must be held down."
                   Foreground="Gray" FontSize="11" Margin="0,2,0,8"/>

        <DockPanel>
            <TextBlock DockPanel.Dock="Right" Width="60" TextAlignment="Right"
                       Text="{Binding HoldDelayLabel}"/>
            <Slider Minimum="{Binding MinHoldDelayMs}"
                    Maximum="{Binding MaxHoldDelayMs}"
                    Value="{Binding HoldDelayMs}"
                    TickFrequency="50" IsSnapToTickEnabled="True"
                    VerticalAlignment="Center" Margin="0,0,10,0"/>
        </DockPanel>

        <CheckBox Content="Launch MacAccents at Windows startup"
                  IsChecked="{Binding LaunchAtStartup}"
                  Margin="0,20,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="Cancel" Width="80" IsCancel="True"
                    Click="OnCancel"/>
            <Button Content="Save" Width="80" Margin="10,0,0,0" IsDefault="True"
                    Click="OnSave"/>
        </StackPanel>
    </StackPanel>
</Window>
